import flux from "..";

class {{name}}ActionCreators {
  constructor() {
    {{#if actions}}this.generateActions({{#each actions}}
      '{{this}}'{{/each}}
    ){{/if}}
  }
}


export default flux.createActions({{name}}ActionCreators);
