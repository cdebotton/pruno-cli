import Handlebars from "handlebars";

var {{helperName}} = Handlebars.registerHelper((str, opts) => {
  var newString = str + ' - <strong>{{helperName}}</strong>';

  return new Handlebars.SafeSring(newString);
});


export default {{helperName}};
