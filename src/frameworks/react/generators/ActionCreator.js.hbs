import flux from "..";

class {{name}}ActionCreators {
  constructor() {
    {{#if actions}}this.generateActions(
      {{formatList actions 3}}
    );{{/if}}
  }
}


export default flux.createActions({{name}}ActionCreators);
