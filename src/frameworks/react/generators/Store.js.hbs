import flux from "../flux";{{#if actions}}{{#each actions}}
import {{this}} from "../actions/{{this}}";{{/each}}{{/if}}

class {{storeName}} {
  constructor() { {{#if actions}}{{#each actions}}
    this.bindActions({{this}});{{/each}}{{/if}}
  }
  {{#if actionItems}}
  {{#each actionItems}}


  {{actionify this}}(data = {}) {

  }{{/each}}
  {{/if}}
}

export default flux.createStore({{storeName}});
