import {View} from "backbone";

var {{viewName}} = View.extend({
    template: require('../hbs/{{templateName}}.hbs'),
    events: {

    },

    initialize() {

    },

    render() {
      var ctx = this.model.toJSON();
      var html = this.template(ctx);

      this.$el.html(html);
      return this;
    }
});

export default {{viewName}};
