import {Collection} from "backbone";
import {{modelName}} from "../models/{{modelName}}.js";

var {{collectionName}} = Collection.extend({
  model: {{modelName}},

  initialize() {

  }
});

export default {{collectionName}};
